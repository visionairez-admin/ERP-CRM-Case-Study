{
  "buildCommand": "npm run build --workspace=backend && npm run build --workspace=frontend",
  "outputDirectory": "frontend/dist",
  "env": {
    "VITE_API_BASE_URL": "@VITE_API_BASE_URL"
  },
  "installCommand": "npm install",
  "framework": "vite"
}
